# preference_chain

Dependency resolution algorithm for action ordering.

The preference chain computes the optimal execution order for actions based on their `depends` and `run_preferably_after` declarations. See the [Architecture](../architecture.md#dependency-resolution) guide for a high-level overview.

::: pyscaf_core.preference_chain
    options:
      show_submodules: true
